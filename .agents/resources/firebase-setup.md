# 📚 Configuración Firebase - Canis Academia

1. **CLI**: Ejecutar `flutterfire configure`.
2. **Colecciones**: Crear la tabla `perros` en la base de datos de producción.
3. **Seguridad**:
   ```javascript
   allow read, write: if request.auth != null;
   ```
